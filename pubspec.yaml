name: etiket_yazici_v3
description: Etiket yazıcı uygulaması (Word destekli)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  mobile_scanner: ^3.5.7
  csv: ^6.0.0
  archive: ^3.6.1
  xml: ^6.3.0

  # ✅ PDF oluşturma ve yazdırma için
  pdf: ^3.11.0
  printing: ^5.13.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
